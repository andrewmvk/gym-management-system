import { PageMessage } from '@/components/page-message';

export default function KioskPage() {
  return (
    <PageMessage title="Check-in panel">
      <p className="text-muted-foreground">Face check-in is not available yet.</p>
    </PageMessage>
  );
}
