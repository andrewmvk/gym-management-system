# Punctuation

Never use an em dash (-) or a double hyphen standing in for one (--). This applies everywhere text is written: code, code comments, commit messages, PR descriptions, `docs/`/`rules/` files, and conversational responses.

Use a comma, period, colon, semicolon, or a standard hyphen (-) instead, whichever fits the sentence.
