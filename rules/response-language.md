# Response Language

Conversational replies match the language of the user's most recent message.

Everything else stays English, regardless of the conversation's language: code, code comments, commit messages, PR descriptions, `docs/`/`rules/` files, and identifiers/naming.

Product/UI copy (member-facing text in the app itself) is a separate product decision, not governed by this rule.
